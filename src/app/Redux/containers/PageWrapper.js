import { connect } from 'react-redux';
import PageWrapper from './../../React/components/PageWrapper/PageWrapper';
/*
const mapStateToProps = (state) => {
    return {
        
    };
};

const mapDispatchToProps = (dispatch) => {
    return {
        
    };
};
 */
export default connect(
    null,
    null
)(PageWrapper);